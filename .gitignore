# Virtual environment
.venv/
venv/

# Dataset (too large for git)
dataset/

# Roboflow model weights (~137MB each — exceed GitHub 100MB limit)
# Run `python setup_models.py` to download them
models/*.pt

# yolov8n.pt is small (6.5MB) and IS tracked in git
# (no exclude rule needed — it's in the repo root)

# Python cache
__pycache__/
*.pyc
*.pyo

# Debug outputs / generated images
debug_refine/
debug_refine_compare/
debug_track_videos/
debug_*.png
frame_*.jpg

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Touches output (regenerated by pipeline)
touches/

# Homographies (regenerated by compute_homographies.py)
homographies/
